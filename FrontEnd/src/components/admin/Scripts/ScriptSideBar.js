// Esta es la función que se ejecuta al pulsar el botón
/*function toggleNavUser() {
  // Selecciona el NavUser por su clase
  var navUser = document.querySelector(".NavUser");

  // Comprueba el valor actual de la propiedad display
  if (navUser.style.display == "none") {
    // Si es none, lo cambia a block
    navUser.style.display = "block";
  } else {
    // Si es otro valor, lo cambia a none
    navUser.style.display = "none";
  }
}*/